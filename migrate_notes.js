const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function migrate() { const leads = await prisma.lead.findMany({ where: { notes: { not: null } } }); for (const lead of leads) { if (lead.notes && lead.notes.trim() !== '') { await prisma.leadNote.create({ data: { content: lead.notes, leadId: lead.id, department: 'Migration' } }); await prisma.lead.update({ where: { id: lead.id }, data: { notes: null } }); } } console.log('Migrated', leads.length, 'leads'); } migrate().finally(() => prisma.$disconnect());
